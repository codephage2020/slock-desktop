name: Landing page branch source

on:
  workflow_dispatch:

jobs:
  branch-source:
    runs-on: ubuntu-latest
    steps:
      - name: Describe publication source
        run: echo "GitHub Pages publishes /docs from codex/landing-page."
